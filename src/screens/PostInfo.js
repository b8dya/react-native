import React, { useEffect, useState } from 'react';
import { ActivityIndicator, FlatList, Text, View, Button, StyleSheet } from 'react-native';

export const PostInfo = ({ navigation }) => {
return(
    <View>
        <Text>postinfo</Text>
    </View>
)


}

PostInfo.navigationOptions = {
    headerTitle: 'Posts',
   
}